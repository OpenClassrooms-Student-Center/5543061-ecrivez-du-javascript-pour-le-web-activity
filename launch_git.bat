@echo off
rem @Author Franck
rem This script allows to add the path pointing to git
rem You can then type > git config user.name "your-email-adrees"
rem and > git config user.name "your-git-name-account"
rem > git config --list
set "PATH=D:\PortableGit\bin;%PATH%"
cmd git.exe